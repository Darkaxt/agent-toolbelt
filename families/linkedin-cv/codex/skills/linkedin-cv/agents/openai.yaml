interface:
  display_name: "LinkedIn CV"
  short_description: "Capture explicit LinkedIn profile evidence locally and compare it with a CV without scraping or automation."
  default_prompt: "Use $linkedin-cv for safe, read-only LinkedIn profile evidence capture and CV/profile gap comparisons grounded in one explicit accessible snapshot."
policy:
  allow_implicit_invocation: true
