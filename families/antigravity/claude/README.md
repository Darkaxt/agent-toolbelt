# Antigravity Claude plugin

The local marketplace contains the `antigravity-cli` plugin. It exposes the
same bounded, exact-model packet review helper as the Codex skill and keeps its
runtime and OAuth state isolated from the existing Claude CLIProxyAPI process.
