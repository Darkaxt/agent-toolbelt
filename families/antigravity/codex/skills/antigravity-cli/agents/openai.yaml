interface:
  display_name: "Antigravity CLI"
  short_description: "Run exact-model packet, public-page, or prepared-video analysis."
  default_prompt: "Use $antigravity-cli to analyze an explicit packet or bounded public evidence with a user-selected exact Antigravity model and fail closed on fallback."
policy:
  allow_implicit_invocation: true
