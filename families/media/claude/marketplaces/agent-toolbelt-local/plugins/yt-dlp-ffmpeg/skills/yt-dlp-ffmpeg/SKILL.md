---
name: yt-dlp-ffmpeg
description: Use this skill when the user wants to download a public media URL, fetch subtitles, inspect a media file, clip media, remux, extract audio, transcode for compatibility, or explicitly asks for `yt-dlp`, `ffmpeg`, or `ffprobe`.
version: 0.1.0
---

# YT-DLP + FFmpeg

Use `scripts/invoke_media_tool.py` for public media download and local file-level media operations. The wrapper delegates into the media family package in this repo.
