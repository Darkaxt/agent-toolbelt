# Outlook Classic Mail Claude Plugin

This family ships a Claude Code plugin under `marketplaces/agent-toolbelt-local`.

The plugin contains one skill and one wrapper script. It delegates through the
Outlook Classic Mail family package and then into the local COM client installed
under the user's local Tools directory. It does not implement COM access itself.
